apiVersion: v1
kind: Service
metadata:
  name: {{ include "tbt-ratio-controller.fullname" . }}
  labels:
    {{- include "tbt-ratio-controller.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "tbt-ratio-controller.selectorLabels" . | nindent 4 }}
